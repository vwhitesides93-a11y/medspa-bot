# (Optional) Use if you want to store non-secret env in a ConfigMap instead of values
# apiVersion: v1
# kind: ConfigMap
# metadata:
#   name: {{ include "medspa-bot.fullname" . }}-config
# data:
#   BOOKING_URL: "{{ .Values.env.bookingUrl }}"
#   CORS_ORIGINS: "{{ .Values.env.corsOrigins }}"
